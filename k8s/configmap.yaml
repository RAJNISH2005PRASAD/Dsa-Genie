apiVersion: v1
kind: ConfigMap
metadata:
  name: dsa-genie-config
  namespace: dsa-genie
data:
  NODE_ENV: "production"
  PORT: "5000"
  AI_PROVIDER: "gemini"
  # MONGO_URI, JWT_SECRET, etc. should be in Secrets
